#!groovy

properties(
    [[$class: 'GithubProjectProperty', projectUrlStr: 'https://github.com/hmcts/RMI-AUTOMIZATION-EMAIL.git'],
     pipelineTriggers([[$class: 'GitHubPushTrigger']])]
)

@Library('Infrastructure')

def type = "java"

def product = "rmi-email"

def component = "batch"

#withPipeline(type, product, component) {
 #enablePerformanceTest()
#}
